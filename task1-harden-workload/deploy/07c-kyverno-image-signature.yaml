apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: require-image-signature
  annotations:
    policies.kyverno.io/title: Require Signed Images
    policies.kyverno.io/severity: high
    policies.kyverno.io/description: >-
      Images must be signed (verified via Cosign keyless signature).
      Enforced once Task 2 CI pipeline signs images with Cosign keyless signing.
spec:
  validationFailureAction: Enforce
  background: false
  rules:
    - name: check-image-signature
      match:
        any:
          - resources:
              kinds:
                - Pod
      exclude:
        any:
          - resources:
              namespaces:
                - argocd
                - kube-system
      verifyImages:
        - imageReferences:
            - "ghcr.io/*"
          mutateDigest: false
          attestors:
            - count: 1
              entries:
                - keyless:
                    subject: "https://github.com/*"
                    issuer: "https://token.actions.githubusercontent.com"
                    rekor:
                      url: https://rekor.sigstore.dev
